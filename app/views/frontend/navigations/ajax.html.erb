<% @navigations.each do |p| %>
<div class="area-bj" >
    <span class="area-title"><%=p[0]%>（<%=p[1].count %>）</span>
    <div class="area-bj-con">
        <% p[1].each do |n|%>
        <%=link_to n.name , frontend_navigation_path(n) %>
        <% end %>
     </div>
 </div>
 <% end %>